# Data Directory

Place the local measurement file here as `DC-IV.csv`.

Expected format:

```csv
voltage,current
0.0,0.0
0.1,1.2e-9
...
```

The scripts use the first two columns, so alternative column names are acceptable. Experimental data are excluded from version control by default.
