# Dynamic Compact Modeling and NGSpice Simulation of Bipolar Memristive Devices

An engineering workflow for transforming measured bipolar resistive-switching behavior into a SPICE-compatible dynamic compact model.

The project combines:

- behavioral compact modeling in NGSpice
- dynamic internal-state equations
- Python-controlled batch simulation
- particle swarm optimization (PSO) for parameter extraction
- frequency, voltage-amplitude, sequence, and repeated-cycle analysis
- quantitative validation of I–V hysteresis and state evolution

## Project Highlights

- Implements a transient-compatible bipolar memristive-device subcircuit.
- Fits 14 model parameters against measured I–V data using a guided, multi-stage PSO workflow.
- Uses weighted objective regions to balance positive, negative, switching-edge, and near-zero behavior.
- Automates NGSpice deck generation, execution, parsing, plotting, and metric export.
- Evaluates frequency dependence, voltage-amplitude dependence, internal-state recovery, loop area, and branch separation.

## Repository Structure

```text
.
├── fit_memristor_pso.py          # Guided PSO parameter extraction
├── analyze_frequency_sweep.py    # Frequency and state-recovery analysis
├── analyze_voltage_sweep.py      # Voltage, sequence, and repeated-cycle analysis
├── models/
│   ├── model_material_rate_state.cir
│   ├── model_direct_rh_state_path.cir
│   └── model_target_state_path.cir
├── data/
│   └── README.md
├── docs/
│   ├── MODEL_OVERVIEW.md
│   └── PUBLICATION_CHECKLIST.md
├── requirements.txt
└── .gitignore
```

## Workflow

```text
Measured I–V data
      ↓
Dynamic compact-model equations
      ↓
NGSpice transient subcircuit
      ↓
Python batch simulation
      ↓
Guided PSO parameter fitting
      ↓
Frequency / amplitude / sequence validation
      ↓
Plots, metrics, and engineering interpretation
```

## Requirements

- Python 3.10 or newer
- NGSpice
- NumPy
- pandas
- Matplotlib

Install the Python packages:

```bash
pip install -r requirements.txt
```

Install NGSpice and ensure the `ngspice` command is available in your system path. On Windows, a local `ngspice.exe` can alternatively be placed in the repository root; the executable is excluded from Git.

## Data Preparation

Place the measurement file at:

```text
data/DC-IV.csv
```

The first two columns must contain voltage and current. The real experimental dataset is intentionally excluded from this repository. Use only data that you have permission to publish.

## Usage

### 1. Fit model parameters

```bash
python fit_memristor_pso.py
```

The fitting program creates:

```text
results/fit/
├── fitdeck_embedded.cir
├── theta_best.csv
├── fit_logs/
└── fitting outputs and figures
```

### 2. Run frequency analysis

```bash
python analyze_frequency_sweep.py
```

The default frequency cases are generated by scaling the transient period and include 0.4, 0.2, 0.1, 0.05, and 0.025 Hz.

### 3. Run voltage-condition analysis

```bash
python analyze_voltage_sweep.py
```

The script supports positive- and negative-amplitude sweeps, multi-negative sequences, repeated cycles, and thesis-oriented comparison metrics.

## SPICE Model Variants

The `models/` directory contains three exploratory state-equation implementations:

1. **Material-rate state model** — explicit SET and RESET rates with smooth activation and boundary protection.
2. **Direct Rh state path** — voltage-dependent dynamic resistance directly drives the internal-state node.
3. **Target-state model** — a smooth polarity target `A(V)` drives the internal state through blended SET/RESET time constants.

These files document the model-development process and the comparison of alternative state-update formulations.

## Reproducibility Notes

The Python scripts were originally developed as thesis-oriented standalone engineering programs. The repository version preserves the validated computational logic while standardizing file names and output paths.

Generated results, logs, local executables, measured data, and fitted parameter files are excluded through `.gitignore`.


## Author

**Chi-Chen Chang (Cora)**  
Compact modeling · SPICE simulation · Python automation · model validation
